import React from 'react';        // for JSX
import ReactDOM from 'react-dom'; // DOM to render components
import App from './Components/App';      // Component to render on the react DOM
import index from './index.css';

ReactDOM.render(<App />, document.getElementById("root")); // render the App component on react DOM